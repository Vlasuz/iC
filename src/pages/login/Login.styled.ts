import styled from "styled-components";

export const LoginStyled = styled.form`
  .btn:disabled {
    background: grey;
    border: grey solid 1px;
    cursor: default;
  }
  p.error {
    color: red;
    font-size: 17px;
  }
`
